<Application x:Class="ComfyX.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- ═══════════════════════════════════════════════════════════════ -->
        <!-- X-Repass RGB Neon Theme — Ported from ComfyX ImGui Theme      -->
        <!-- ═══════════════════════════════════════════════════════════════ -->

        <!-- Background Colors -->
        <Color x:Key="BgDeepColor">#FF0A0A12</Color>
        <Color x:Key="BgPrimaryColor">#FF12121F</Color>
        <Color x:Key="BgSecondaryColor">#FF16162A</Color>
        <Color x:Key="BgElevatedColor">#FF1A1A2E</Color>
        <Color x:Key="BgPanelColor">#EB06060C</Color>
        <Color x:Key="BgInputColor">#FF0D0D1A</Color>
        <Color x:Key="BgCardColor">#FF040408</Color>

        <!-- Border Colors -->
        <Color x:Key="BorderColor">#FF2A2A4A</Color>
        <Color x:Key="BorderSubtleColor">#FF1A1A30</Color>
        <Color x:Key="BorderGlowColor">#FF3A3A5A</Color>

        <!-- Neon Accent Colors -->
        <Color x:Key="NeonCyanColor">#FF00F5FF</Color>
        <Color x:Key="NeonCyanHoverColor">#FF66F9FF</Color>
        <Color x:Key="NeonCyanMutedColor">#1F00F5FF</Color>
        <Color x:Key="NeonPinkColor">#FFFF00FF</Color>
        <Color x:Key="NeonPurpleColor">#FFBF00FF</Color>

        <!-- Semantic Colors -->
        <Color x:Key="SuccessColor">#FF00FF88</Color>
        <Color x:Key="ErrorColor">#FFFF3366</Color>
        <Color x:Key="WarningColor">#FFFF6B35</Color>

        <!-- Text Colors -->
        <Color x:Key="TextPrimaryColor">#FFE0E0E0</Color>
        <Color x:Key="TextSecondaryColor">#FF808090</Color>
        <Color x:Key="TextMutedColor">#FF4A4A5A</Color>

        <!-- Solid Brushes -->
        <SolidColorBrush x:Key="BgDeepBrush" Color="{StaticResource BgDeepColor}" />
        <SolidColorBrush x:Key="BgPrimaryBrush" Color="{StaticResource BgPrimaryColor}" />
        <SolidColorBrush x:Key="BgSecondaryBrush" Color="{StaticResource BgSecondaryColor}" />
        <SolidColorBrush x:Key="BgElevatedBrush" Color="{StaticResource BgElevatedColor}" />
        <SolidColorBrush x:Key="BgPanelBrush" Color="{StaticResource BgPanelColor}" />
        <SolidColorBrush x:Key="BgInputBrush" Color="{StaticResource BgInputColor}" />
        <SolidColorBrush x:Key="BgCardBrush" Color="{StaticResource BgCardColor}" />

        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
        <SolidColorBrush x:Key="BorderSubtleBrush" Color="{StaticResource BorderSubtleColor}" />
        <SolidColorBrush x:Key="BorderGlowBrush" Color="{StaticResource BorderGlowColor}" />

        <SolidColorBrush x:Key="NeonCyanBrush" Color="{StaticResource NeonCyanColor}" />
        <SolidColorBrush x:Key="NeonCyanHoverBrush" Color="{StaticResource NeonCyanHoverColor}" />
        <SolidColorBrush x:Key="NeonCyanMutedBrush" Color="{StaticResource NeonCyanMutedColor}" />
        <SolidColorBrush x:Key="NeonPinkBrush" Color="{StaticResource NeonPinkColor}" />
        <SolidColorBrush x:Key="NeonPurpleBrush" Color="{StaticResource NeonPurpleColor}" />

        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}" />
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" />

        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}" />
        <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}" />

        <!-- Gradient Brushes -->
        <LinearGradientBrush x:Key="CyanPinkGradient" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="{StaticResource NeonCyanColor}" Offset="0" />
            <GradientStop Color="{StaticResource NeonPinkColor}" Offset="0.5" />
            <GradientStop Color="{StaticResource NeonPurpleColor}" Offset="1" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="PinkCyanGradient" StartPoint="0,0.5" EndPoint="1,0.5">
            <GradientStop Color="{StaticResource NeonPinkColor}" Offset="0" />
            <GradientStop Color="{StaticResource NeonCyanColor}" Offset="0.5" />
            <GradientStop Color="{StaticResource NeonPurpleColor}" Offset="1" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="CyanButtonGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FF0A3D5C" Offset="0" />
            <GradientStop Color="#FF1E3A5F" Offset="1" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="PrimaryButtonGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FF006B7F" Offset="0" />
            <GradientStop Color="#FF004D5C" Offset="1" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="DangerButtonGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FF5C1A1A" Offset="0" />
            <GradientStop Color="#FF3D0A0A" Offset="1" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="SuccessButtonGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FF0A3D2A" Offset="0" />
            <GradientStop Color="#FF1A5C3A" Offset="1" />
        </LinearGradientBrush>

        <!-- ═══════════════════════════════════════════════════════════════ -->
        <!-- Control Styles                                                 -->
        <!-- ═══════════════════════════════════════════════════════════════ -->

        <!-- Modern Button -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource CyanButtonGradient}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource NeonCyanBrush}" />
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{StaticResource NeonCyanColor}" BlurRadius="12" ShadowDepth="0" Opacity="0.4" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BgElevatedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button (Cyan/Teal) -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource PrimaryButtonGradient}" />
            <Setter Property="Foreground" Value="{StaticResource NeonCyanBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <!-- Danger Button (Red) -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource DangerButtonGradient}" />
            <Setter Property="Foreground" Value="{StaticResource ErrorBrush}" />
        </Style>

        <!-- Success Button (Green) -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource SuccessButtonGradient}" />
            <Setter Property="Foreground" Value="{StaticResource SuccessBrush}" />
        </Style>

        <!-- Pink Button -->
        <Style x:Key="PinkButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FF5C0A3D" Offset="0" />
                        <GradientStop Color="#FF3D0A2A" Offset="1" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{StaticResource NeonPinkBrush}" />
        </Style>

        <!-- Sidebar Nav Button -->
        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Margin" Value="0,4" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Rectangle x:Name="activeBar" Width="3" HorizontalAlignment="Left" RadiusX="1.5" RadiusY="1.5"
                                       Fill="Transparent" Margin="-10,6,0,6" />
                            <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#15FFFFFF" />
                                <Setter TargetName="bg" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{StaticResource NeonCyanColor}" BlurRadius="8" ShadowDepth="0" Opacity="0.2" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TopBar Button -->
        <Style x:Key="TopBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#20FFFFFF" />
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BgInputBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="CaretBrush" Value="{StaticResource NeonCyanBrush}" />
            <Setter Property="SelectionBrush" Value="{StaticResource NeonCyanMutedBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource NeonCyanBrush}" />
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{StaticResource NeonCyanColor}" BlurRadius="10" ShadowDepth="0" Opacity="0.3" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Neon Card Border -->
        <Style x:Key="NeonCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BgCardBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="16" />
        </Style>

        <!-- Log TextBox (Terminal Style) -->
        <Style x:Key="LogTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource SuccessBrush}" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        </Style>

        <!-- Modern ComboBox -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource BgInputBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <!-- Modern CheckBox -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <!-- Modern TabControl -->
        <Style x:Key="ModernTabItem" TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" Background="Transparent" CornerRadius="8,8,0,0"
                                Padding="{TemplateBinding Padding}" Cursor="Hand">
                            <ContentPresenter x:Name="content" ContentSource="Header"
                                              HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BgElevatedBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource NeonCyanBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#15FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
